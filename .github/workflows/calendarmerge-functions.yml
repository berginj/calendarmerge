name: Deploy Calendar Merge

on:
  push:
    branches: ["main"]
    paths:
      - "src/**"
      - "public/**"
      - "frontend/**"
      - "scripts/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/calendarmerge-functions.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  test_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install backend dependencies
        run: npm ci

      - name: Run backend tests
        run: npm test

      - name: Build backend deployment package
        shell: pwsh
        run: ./scripts/azure/package-functions.ps1

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Azure login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_4AFC0F50DAC647279FA31EE99554BFE6 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_998DD97F26E54CC581D749EF843AAA1D }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_F56E93C49C17421FA862E7A2D86B6ED6 }}

      - name: Deploy Function App
        run: >
          az functionapp deployment source config-zip
          --resource-group "${{ vars.AZ_RESOURCE_GROUP }}"
          --name "${{ vars.AZ_FUNCTIONAPP_NAME }}"
          --src ".artifacts/calendarmerge-functions.zip"

      - name: Deploy Frontend to Blob Storage
        run: |
          az storage blob upload \
            --account-name "${{ vars.AZ_STORAGE_ACCOUNT }}" \
            --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
            --container-name '$web' \
            --name "index.html" \
            --file "./public/index.html" \
            --overwrite true \
            --content-type "text/html; charset=utf-8"

          az storage blob upload \
            --account-name "${{ vars.AZ_STORAGE_ACCOUNT }}" \
            --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
            --container-name '$web' \
            --name "games.html" \
            --file "./public/games.html" \
            --overwrite true \
            --content-type "text/html; charset=utf-8"

          az storage blob upload \
            --account-name "${{ vars.AZ_STORAGE_ACCOUNT }}" \
            --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
            --container-name '$web' \
            --name "games" \
            --file "./public/games.html" \
            --overwrite true \
            --content-type "text/html; charset=utf-8"

          az storage blob upload \
            --account-name "${{ vars.AZ_STORAGE_ACCOUNT }}" \
            --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
            --container-name '$web' \
            --name "games/index.html" \
            --file "./public/games.html" \
            --overwrite true \
            --content-type "text/html; charset=utf-8"

          az storage blob upload-batch \
            --account-name "${{ vars.AZ_STORAGE_ACCOUNT }}" \
            --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
            --destination '$web/manage' \
            --source "./frontend/build" \
            --overwrite true
